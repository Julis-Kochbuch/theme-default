name: Publish new theme version

on:
  push:
    branches: [ "main" ]

jobs:
  publish:
    uses: Julis-Kochbuch/github-actions/.github/workflows/publish-theme-update.yml@main
    permissions:
      contents: write
      pages: write
      id-token: write
